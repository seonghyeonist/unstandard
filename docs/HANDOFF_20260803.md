# Handoff — Critical Audit and Preview Validation

## Current state

Work is prepared on `agent/p0-critical-audit-20260803`, based on
`cf87a2f2273385efe49d7314f76ceae231b0f305` from
`cursor/neon-drizzle-better-auth-rebuild-909d`.

The source fixes and local gates are complete. The intended GitHub artifact is
a separate draft PR; existing PR #63 must not be overwritten because it records
an older P0.4G documentation state.

## Do not infer from local PASS

- `npm test` is a static/unit/contract pass, not database proof.
- The current private-profile and unlock surfaces are still mock-backed; they
  fail closed in database runtime rather than claiming Neon authorization.
- Local BGE-M3/threshold artifacts and AI A/B labels are not human ground truth.
- Alpha remains `BLOCKED_EXTERNAL`.

## Next operator sequence

1. Confirm the pushed branch commit and draft PR target before review.
2. Wait for a Vercel Preview deployment whose commit SHA exactly matches the PR
   head; inspect build logs and public unauthenticated routes.
3. For disposable integration proof, set a separate `TEST_DATABASE_URL`, use
   `DATABASE_ENV=test`, and set the repository's explicit destructive-test
   confirmation. Never point this at Preview or Production.
4. For Preview proof, use a separate Preview Neon branch/database. Apply only
   the documented staging migration/seed commands; record the resulting
   machine-generated evidence without credentials, cookies, emails, or raw
   profile answers.
5. Supply two approved Preview test users and the Vercel protection bypass only
   through the secret-handling path. Run the authorization smoke matrix and
   verify the deployed SHA before building combined readiness evidence.
6. Keep Production untouched until the owner makes a separate promotion
   decision after all P0 gates pass.
7. Complete the independent human-label workflow: same canonical sample,
   reviewer 1 and reviewer 2, conflict adjudication, and a redacted evidence
   artifact. Do not generate labels with AI to fill the gate.

## Required owner inputs if blocked

| Input | Why it is needed |
|---|---|
| `TEST_DATABASE_URL` for a disposable test DB | Real PostgreSQL integration proof |
| Separate Preview DB access/branch | Preview migration, seed, and HTTP proof |
| Two Preview test-user credentials | A/B authorization smoke |
| Vercel protection bypass, if enabled | Reach the Preview HTTP surface non-interactively |
| Two independent human reviewers | Human-label gate |

Only provide secret values through the configured secret mechanism. Report
missing variable names, never secret values.
