/** Alpha-global unlock question pointer (Option B). Not a per-profile mapping. */
export const UNLOCK_QUESTION_CONFIG_KEY = "unlock.active_question_id" as const;

/** Seeded unlock question id — distinct from onboarding question (RFC UUID variant). */
export const DEFAULT_UNLOCK_QUESTION_ID = "33333333-3333-4333-8333-333333333333";
