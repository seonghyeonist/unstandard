import type { ReportTargetType } from "@/types/api";

export async function reportTarget(targetType: ReportTargetType, targetId: string, reason: string): Promise<void> {
  void targetType;
  void targetId;
  void reason;
}
